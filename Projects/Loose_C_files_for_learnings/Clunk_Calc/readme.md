# Following along the fast bit embedded basic C course

math.c ---> holding the actinf functions
math.h ---> holding the function prorotypes
main.c ---> holding the calls to the function

### compile with gcc main.c math.c -o clunk_calc
